# enthuse

## Personal Android app bridge (automatic install)

Run:

```bash
bash tools/android_app_bridge.sh
```

This version auto-installs dependencies and auto-installs your APK by default.

See details in:

- `docs/android_single_app_bridge.md`
